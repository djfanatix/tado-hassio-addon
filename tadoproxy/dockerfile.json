# Dockerfile
FROM ghcr.io/s1adem4n/tado-api-proxy:latest

# Set the cookie file to a writable location inside the container
ENV COOKIE_FILE=/data/cookies.json

# Just run the binary from the original image
CMD ["/tado-api-proxy"]